{
  "name": "koreaplus-webapp",
  "version": "1.0.0",
  "private": true,
  "description": "KoreaPlus Travel Guide — Cloudflare Worker backend",
  "scripts": {
    "dev": "wrangler dev src/worker.ts --port 8787",
    "deploy": "wrangler deploy src/worker.ts",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.80.0"
  }
}
