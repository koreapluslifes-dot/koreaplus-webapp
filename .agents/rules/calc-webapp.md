---
trigger: model_decision
description: "Rule for koreaplus-webapp: calc-webapp.md"
---
New webapp at `C:\Users\juksu\calc-webapp` (own git repo) — global Calculator Hub served at **all-lifes.com/calc/**. Plan doc: `C:\tmp\all-lifes-repo\docs\calculator-hub-plan.md`.

**Architecture:** JSON-defined calculators → static multilingual pages. `calculators/{slug}.json` (data + en/ko i18n) + `formulas/{slug}.js` (pure fn + inline TESTS). `build.cjs` generates `dist/calc/{lang}/{category}/{slug}/` + hubs + sitemap + hreflang + schema.org. `assets/calc-core.js` = shared runtime (live calc, showWhen conditional inputs, URL-state share, copy/share/embed, PWA). Vanilla JS, no framework.

**Deploy = Lightsail direct**, NOT via WordPress plugin or Cloudflare Worker (user explicitly chose this). `deploy.ps1` tars `dist/calc/*` → scp → extract to `/opt/bitnami/wordpress/calc` + `chmod -R a+rX` (bitnami 403 fix). Reuses same Lightsail server as lucky/iq (deploy.config.ps1 gitignored; only SSH_PATH differs).

**Status (2026-07-06): DEPLOYED & LIVE at all-lifes.com/calc/ — 1,126 URLs.** deploy.config.ps1 filled (same box 3.36.235.171, PEM C:\Users\juksu\LightsailDefaultKey-ap-northeast-2.pem, INDEXNOW key present). Redeploy: `.\deploy.ps1` (test+build+scp+chmod+IndexNow, ASCII-only script — PS5.1 mangles UTF-8).

Two page families in one pipeline: (1) **9 formula calculators** (percentage, bmi, age, date-difference, loan-payment, compound-interest, tip, discount, calorie-bmr) — en+ko only so far; (2) **conversion engine** — `measures/{id}.json` (units+factors, affine offset supported for temperature) → measure converter page + N pairwise pages (km-to-mi, c-to-f…) with real value tables. 12 measures (length, mass, temperature, area, volume, speed, data, time, pressure, energy, power, angle), 108 pairwise/lang, **localized to all 9 langs** (en,ko,ja,de,es,fr,pt,it,id) via `conv-i18n.json` (shared framework strings, written once/lang) + per-measure unit names. 77 formula/conversion tests pass (test/run-tests.cjs CHECKS spot-checks every measure). Cache-bust: assets carry ?v=<build> + SW cache name per build.

**Content depth (2026-07-06, tripled):** calculators (en/ko) carry `i18n.<lang>.sections[]` (rich `{h,p[],table?,list?}` blocks: WHO BMI bands, amortization cost table, Rule of 72, TDEE activity multipliers, world tipping norms, discount math, Korean-age system…) + 5–7 FAQs. Measures carry `i18n.<lang>.explainer[]` (SI definitions/history/precision), `references[]` (`{value,label}` handy facts — **value is identical across all langs**, only label translated), and per-unit `units.<u>.def`. Pairwise pages render unit-defs + a **reverse** conversion table + a worked example. build.cjs renderers: `renderSections()`, `renderReferences()`; conv-i18n framework keys `aboutUnits`/`referencesTitle`/`worked`. Enrichment translated to 7 langs via parallel agents (frame as "generate i18n locale resource"; agents save to varied paths — scratchpad/repo-root/C:\tmp, gather from all).

**v2 (2026-07-09): design system v2 + pro tools — 1,146 URLs live.** style.css fully redesigned (gradient hero, soft-elevation cards, solid sticky header — NO backdrop-blur per [[scroll-perf-pc]], dark mode, section markers); header has brand-mark + language switcher (`#lang-sel`, populated client-side from hreflang alternates). **Scientific calculator** at `/{lang}/math/scientific-calculator/` ×9 langs: `formulas/sci-engine.js` (shunting-yard; unary-minus must NOT pop binary ops — fixed bug; 26 tests) + `assets/sci-calc.js` (keypad/live-preview/ANS/history/keyboard) + `tools-i18n.json` (guide+function-reference+FAQs; 7 langs translated via **Workflow parallel agents with schema** — far cleaner than free-text agents; result JSON at wrapper `.result` in the task output file). New calculators: statistics (free-text list input — build supports `type:"text"` + placeholder) and quadratic-equation (complex roots as text output), en/ko. 112 tests.

**AdSense + i18n nav (2026-07-09):** `build.cjs` `ADS` const = publisher `ca-pub-1378943893051810` + top slot `4521899200` / bottom slot `8233374508` (reused all-lifes display units — serve immediately; swap for calc-specific units later). Top+bottom responsive `<ins>` banners render on CONTENT pages only (calc/measure/pair/sci); home hubs + category hubs get `ads:false` (no loader, no units — respects "홈 제외"). AdSense loader gated in `head()` on `o.ads!==false`; footer script pushes each `ins.adsbygoogle`. **Language switcher fixed the "only en/ko visible" complaint:** the 11 calculators are en/ko-only so their hreflang alternates (and thus the old switcher) only listed 2 langs — users couldn't reach the 9-lang content. Now header `<select>` + footer row ALWAYS list all 9 `SITE_LANGS`: equivalent page when an hreflang alternate exists, else that language's home `/calc/{lang}/`. All 9 language pages verified live (ja/de/es/fr/it/pt/id return 200 with correct `<html lang>` + localized H1).

**To grow further:** add measures (pure data, incl. their CHECKS in run-tests) or add ja/de/es/fr/pt/it/id i18n to the 11 calculators (currently en/ko) → each calc×lang = +1 page (also makes their switcher point to equivalent pages instead of home). Headless preview quirk: viewport reports 0x0 until `preview_resize` with explicit width/height — layout "bugs" at 0-width are artifacts. Language policy: build only emits (item×lang) pages where i18n exists — never thin auto-translated. **Gotcha:** general-purpose agents may REFUSE "translate this" framing; frame as "generate an i18n locale resource file for the build". Related: [[app-redesign-and-detail-l10n]], [[deploy-seo-pages]].
