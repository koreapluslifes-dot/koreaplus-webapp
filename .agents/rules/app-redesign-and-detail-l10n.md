---
trigger: model_decision
description: "Rule for koreaplus-webapp: app-redesign-and-detail-l10n.md"
---
Work on the main webapp UI (the index.html SPA — dispatch-shared files), done on branch **`redesign/app-ui`**, deployed to live, and **fast-forward-merged into main + pushed (commit 3f59896)** — repo source now matches live. (The branch also carried a few dispatch commits that had landed on it because the working dir is shared between sessions; the FF merge settled them into main too. Dispatch's *uncommitted* WIP — build-seo.cjs, kpop*, blog, src/router.ts, src/api/aliexpress.ts — was left untouched throughout.)

**Follow-ups (also live + on main):** bottom homepage AdSense removed too (home is now ad-free); redesign pass-2 on the home Must-Visit + bucket-list sections (1240 width, 16-radius, fixed `.mv-tags` black-on-dark contrast); price.range strings localized per language (Budget/Mid/Luxury → e.g. ko 알뜰/중급/럭셔리, kept ₩/numbers/proper nouns) via an 8-agent workflow + merged as `priceRange` into each detail-l10n file; an 8-language adversarial QA pass caught + fixed 2 issues (fr 한옥/한복 mixup, pt plural). **Hub-page CSS tone unification — DONE** (commit dde8704): refined `.hub-hero`/`.hub-card` (16-radius)/`.cards-grid` in hub-styles.css and bumped `hub-styles.css?v=9` on the 12 general hub pages (festivals, seasons, culture, temples, nightviews, kdrama-locations, phrases, currency, subway, menu-translator, etiquette, emergency). Crucially did NOT touch `:root` tokens, so the 700+ SEO pages (still ?v=7) + dispatch's kpop(?v=8)/kbeauty(?v=5) pages keep cached CSS = visually unaffected. Also fixed a pre-existing mobile bug: added `html{overflow-x:clip}` to stop the kp-pnav off-screen drawer (position:fixed) from causing horizontal scroll on hub pages — clip preserves the sticky hub-nav (verified). Per-page hardcoded `<link>` (header.js does NOT inject hub-styles), so cache-bust = bump each page's ?v=. **Homepage tested clean for this drawer overflow (style.css has body overflow-x:hidden) — not touched.**

**Redesign pass 1 (core surfaces, mobile-first)** — `style.css` (bumped to `?v=20` in index.html):
- Fixed the broken explore card action row: stale `.card-map-btn{width:100%}` had squeezed the detail button to 34px → CJK label rendered vertically. Card is now flex-column; detail btn flex+ellipsis, map btn fixed; tags use `var(--surface)` (were invisible black-on-dark); 3-line desc clamp.
- Explore: 1240 container, clamp title, flat dark pills + scroll-fade. Hero: tighter type, 46px CTAs. Header: hide duplicate `.ai-btn` ≤960px so nav doesn't overflow/clip in verbose langs (JA).
- Theme toggle verified: all colors use light/dark-adaptive tokens (theme.css `html.light` vs hub-styles.css dark `:root`, loaded last = default dark). NOTE the dual-stylesheet token system.

**Ads** — homepage big AdSense removed: added `data-ads="off"` to `#kp-topads` in index.html (stops `topads.js` adsense(); AliExpress self-hides when keyless). The bottom in-content AdSense (index.html ~line 423) was kept for revenue.

**Detail-panel localization** — `detail-panel.js` now merges per-language `window.DETAIL_L10N` over the English `DETAIL_DATA` base (prose localized; price.range + links kept) and lazy-loads `detail-l10n.<lang>.js` for the current `<html lang>` on panel open (English fallback if missing). Bumped `detail-panel.js?v=8`. Generated `detail-l10n.{ko,ja,zh,es,fr,de,pt,id}.js` (85 items each, key-validated 85/85, ~150–200KB) via two 32-agent localization workflows that translated chunked English content from detail-data.js (chunks were pre-extracted to C:/tmp/dd/). See [[cross-domain-growth-elements]], [[affiliate-layout]], [[deploy-seo-pages]].

**Pending / next:** merge `redesign/app-ui` → main; optional redesign pass 2 (Must-Visit/bucket-list sections, hub pages); optionally remove the bottom homepage AdSense too; price.range strings still have English labels ("Budget/Mid/Luxury").
