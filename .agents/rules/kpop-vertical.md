---
trigger: model_decision
description: "Rule for koreaplus-webapp: kpop-vertical.md"
---
`koreaplus-lifes.com` 은 **별도 저장소** `C:\Users\juksu\koreaplus-webapp`(GitHub: koreapluslifes-dot/koreaplus-webapp, lucky 와 무관) 의 **한국 여행 가이드** 웹앱이다. 스택은 lucky 와 유사: Cloudflare Worker(`src/worker.ts`→`router.ts`→`src/api/*.ts` 클라이언트, KV `CACHE_KV` 캐시 `withCache`, `schema.ts` ApiResponse<T>, 키 없으면 errorResponse(503) 우아한 폴백) + 정적 프론트(루트 허브페이지 culture.html·kdrama-locations.html 등, `modules/*.js` ES모듈, `data.js`/`detail-data.js`, `build-seo.cjs` 가 `/guide/` 크롤 페이지+sitemap+JSON-LD 생성). 9개 언어 `messages/<lang>.json`(ko,en,ja,zh,es,fr,de,pt,id)+`modules/i18n.js`. 수익화: AdSense(ca-pub-1378943893051810)·Trip.com/Agoda/Klook·Impact.com. 배포는 **GitHub Actions**(.github/workflows/deploy-worker.yml, push→main, tsc --noEmit 게이트) — lucky 의 deploy-all.ps1 과 다름. 프론트는 서버(Lightsail) `/guide/` 정적 서빙, 프론트→워커는 `config.js` 의 `window.WORKER_URL`.

**진행중 작업(2026-06-14 착수): `/k-pop` 플래그십 버티컬** — 전세계 실시간 K-pop 허브(근황·컴백·차트·이슈) + 공연→여행→쇼핑 퍼널. 설계 산출물(8-에이전트 워크플로): `C:\Users\juksu\AppData\Local\Temp\claude\C--Users-juksu-lucky-webapp\b2026905-1d53-4d6f-8e97-dd02f2068e7c\tasks\wqfj8qsua.output`.

**API 목록(사용자가 직접 발급):** P0 = YouTube Data API v3(`YOUTUBE_API_KEY`, search.list 금지-쿼터 10k/일), Spotify Web API(Client Credentials, `SPOTIFY_CLIENT_ID`+`SECRET`, 2025.05~ 개발계정 Premium 필요), NewsData.io(`NEWSDATA_API_KEY`, 무료티어 상업이용 허용-GNews/NewsAPI는 금지), Apple iTunes RSS(키없음·유일한 무료 합법 실시간 차트), Wikidata REST(키없음·9개국어 약력, User-Agent 필수). P1 = Ticketmaster Discovery(`TICKETMASTER_API_KEY`)+Impact 제휴, AliExpress 제휴(iq-test-webapp 프록시 포팅). P2 = YesStyle(ShareASale), Last.fm. **Ktown4u/Weverse/Interpark/Makestar 제휴 프로그램 없음**(정보링크만). **K-pop 공식 차트 API 없음**(Circle/Hanteo/Melon 폐쇄·스크래핑 금지) → iTunes RSS + Spotify인기도 + YouTube조회수증분 으로 랭킹 파생.

**⚠️ CI 배포 막힘(2026-06-14 확인):** GitHub Actions 워커배포는 GH시크릿 `CF_API_TOKEN` 에 **Workers KV Storage:Edit 권한 없어 실패**(`code 10023 kv bindings require kv write perms` — 워커가 CACHE_KV 바인딩). 타입체크는 통과, deploy 단계에서만 실패. **해결: CF 대시보드에서 토큰에 KV write 추가 후 GH시크릿 갱신.** 우회(현재 사용): 로컬 `npx wrangler deploy src/worker.ts` — 로컬 wrangler 는 jeybeeicon@gmail.com OAuth(account 9ba6054d…, `workers_kv write` 보유)라 성공. 2026-06-14 워커 버전 cea1bc33 로 /api/kpop/* 라이브 검증완료(charts·bio·health OK).

**배포(2-파트):** ①워커(/api/kpop 라우트) = `git push main` → GitHub Actions(`npm ci`→`npm run typecheck` 게이트→`wrangler deploy src/worker.ts`) **단 위 KV권한 이슈로 현재는 로컬 wrangler deploy 사용**. 신규 API 키는 `wrangler secret put <NAME>`(또는 CF 대시보드) 별도. 워커 URL `koreaplus-webapp.jeybeeicon.workers.dev`(config.js `window.WORKER_URL`). ②정적파일(kpop.html 등) = GH Actions 아님 → `deploy-to-guide.ps1 -ServerIP 18.207.55.50` 로 Lightsail(us-east-1a, 고정IP **18.207.55.50**, 인스턴스 KoreaPlus-Lifes250508) `/opt/bitnami/wordpress/guide/` 에 scp(PEM: `C:\Users\juksu\Documents\blog\koreaplus-lifes\LightsailDefaultKey-us-east-1.pem`). 사이트는 `koreaplus-lifes.com/guide/...`. deploy-to-guide.ps1 의 ROOT/MODULE 배열에 신규 파일 추가해야 업로드됨(kpop 파일 추가 완료). **⚠️ 서버 권한 함정:** `/guide/modules/`·`/guide/messages/` 기존파일이 다른 소유자라 scp 시 "Permission denied" 발생 전례 → 배포 전 `ssh -i <PEM> bitnami@18.207.55.50 "sudo chown -R bitnami:daemon /opt/bitnami/wordpress/guide && sudo chmod -R ug+rw /opt/bitnami/wordpress/guide"` 실행하면 해결(bitnami 는 NOPASSWD sudo). **kpop 허브 2026-06-14 라이브 배포 완료**: `https://koreaplus-lifes.com/guide/kpop.html` (전 자산 HTTP 200 검증).

**Phase 1 완료(2026-06-14, 미커밋/미배포):** 백엔드 6클라이언트(`src/api/`: itunes·wikidata=키없음, spotify·youtube·newsdata·ticketmaster=키가드) + 7라우트(router.ts `/api/kpop/{charts,ticker,bio,artist,feed,concerts}`+health) tsc클린. 데이터 `kpop-data.js`(34팀, 외부ID는 빈값-검증에이전트 rate-limit으로 미완 → Phase 2에서 Wikidata QID 백필해야 약력 표시). 프론트 `kpop.html`+`modules/kpop.js`(실시간 티커·차트·카운트다운타이머·바이어스팔로우 localStorage `kp_kpop_follow`·아티스트모달)+`modules/kpop-sharecard.js`(캔버스 세로 공유카드). i18n 9개언어×29키(en/ko/ja 번역, 나머지 영어폴백-추후 현지화). nav 15개 허브+홈+build-seo PRIMARY_NAV. sw.js v14→v15. sitemap 245URL(kpop.html changefreq daily). 수익화 affiliate.ts pickOffers 에 `kpop`/`concert` 케이스(Klook콘서트→Trip.com호텔→AREX→Agoda) + AdSense slot 4521899200. 검증: tsc, node --check, JSON 9개 유효.

**Top-10 요소 로드맵(2026-06-14, 4-에이전트 팀 워크플로 산출):** 산출물 `…/tasks/wfpcjjo4d.output`. Top10 = ①통합캘린더(컴백+생일+기념일+투어,TZ인식,.ics) ②실시간차트+48h무버스(국가별 storefront+델타) ③아티스트프로필(9국어 위키약력+멤버카드) ④바이어스개인화 ⑤공연→여행퍼널 ⑥스트리밍·투표허브(신규,키없음) ⑦파트분배 SVG(신규,키없음,AdSense안전) ⑧뉴스피드 ⑨MV마일스톤(YouTube키) ⑩팬덤아이덴티티(팬카드). **Phase 0 선행:** ✅kpop.js STR 9개언어 완성(es/fr/de/pt/id/zh 추가, 라이브) ✅생성페이지 og:locale ✅(진행)외부ID 백필(wikidata/spotify/youtube → kpop-ids.verified.json) ⏳KPOP_UPCOMING 2026 컴백 채우기. 차트 storefront는 lang별(ja→jp,pt→br,id→id,zh→tw…) 매핑. 미구현 요소는 위 산출물 PART2 스펙 참조.
**빌드 진행(2026-06-14):** ✅#1 ID/약력 — 34팀 전부 wikidataId(키없는 위키약력 라이브), spotifyId 27/34·youtubeChannelId 26/34(jungkook은 공식채널無). 검증 스크립트 `C:\tmp\merge-kpop-ids2.cjs <json>`(비어있지않은값만 머지). ✅#2 차트 — `kpop.js` STORE_FOR_LANG/CHART_STR/buildChartControls/loadCharts(store,type)+chartDeltas(localStorage 'kp_chart_<store>_<type>' 방문대비 ▲▼)+is-bias 하이라이트+KR폴백; `kpop.html` #kpop-chart-controls; `kpop.css` .kp-store-btn/.kp-ctab/.kp-delta-*. Apple RSS most-played 는 일부 storefront 간헐 타임아웃(워커 8s)이라 KR폴백 필수. ✅#1캘린더 .ics(VALARM -P1D) Add-to-Calendar 카운트다운 카드에. ⏳enrich 에이전트 → `kpop-enrich.json`(멤버생일·팬덤색/응원봉·나머지7팀 spotify/youtube) → 머지 후 #1 월그리드+생일, #10 팬카드, #6 스밍/투표, #7 파트분배 빌드 예정.

**다국어 SEO(hreflang) 완료(2026-06-14):** 전 허브페이지(kpop·culture·festivals·temples·nightviews·kdrama·seasons·phrases·currency·subway·menu-translator·etiquette·emergency·about·contact·plan·explore + index·kbeauty)에 **hreflang 클러스터(x-default+9개언어 `?lang=XX`) + og:locale(9)** 적용. kpop/standalone 은 `/kpop` 기준, 나머지는 각 `/guide/<page>.html` 기준. **서브태그는 bare `zh`/`pt`**(일반 번역 도달 최대화, index/kbeauty도 소급); 단 og:locale 은 `zh_CN`/`pt_BR`(OG 스펙상 territory 필수). **`modules/i18n.js` 에 `syncCanonical()` 추가** — `applyTranslations()`에서 현재 lang 기준으로 `<link rel=canonical>`+`og:url`을 `?lang=XX`(en은 무param)로 동적 갱신(authored canonical 베이스 사용 → /kpop 유지). **`<html lang>`**는 i18n.js가 이미 갱신. 사이트맵의 kpop loc = 클린 `/kpop`(build-seo STATIC에서 kpop.html 제거 + 전용 `urlEntry('/kpop',...)` 추가). **explore.html 은 build-seo.cjs `shell()` 생성물** — hreflang은 explore writePage 호출에 `alts:[...8langs...]` 전달로 해결(파일 직접 수정 금지, 재생성시 덮어써짐). 주입은 `C:\tmp\inject-hreflang.cjs`(canonical 뒤 삽입, idempotent, UTF-8). 생성페이지 og:locale 추가는 미적용(선택 개선).

**⚠️ 배포 권한 재발 함정:** `/opt/bitnami/wordpress/guide/` 파일 소유권이 중간 배포로 비-bitnami로 리셋되어 scp "Permission denied" 재발 → **deploy-to-guide.ps1 전에** `ssh bitnami@18.207.55.50 "sudo chown -R bitnami:daemon /opt/bitnami/wordpress/guide && sudo chmod -R ug+rw /opt/bitnami/wordpress/guide"` 먼저 실행. explore.html 은 ROOT_FILES에 없어 별도 scp 필요. CF는 HTML 캐시 안 함(쿼리 버스트로 즉시 전파 확인).

**광고 모델 확정 + 롤아웃 완료(2026-06-18, 라이브 검증):** 사용자 지시 = **AdSense + AliExpress 광고만**(쿠팡파트너스 전면 제거). `modules/kbeauty.js` 의 `injectCoupang()`·isKo 쿠팡카드 삭제, `kbeauty.html`/`kpop.css` 의 죽은 `.kb-ad-coupang`/`.kp-coupang` CSS·주석 정리. (단 data.js·detail-data.js·places/coupang.html 의 쿠팡 언급은 *편집성 콘텐츠*=한국 쇼핑서비스 소개라 유지.) **상단 광고 블록** = `modules/topads.js`(자체 CSS 주입, `#kp-topads` 컨테이너의 `data-q`=상품쿼리, `data-ads="off"`면 AdSense 생략하고 AliExpress만). AliExpress 스트립은 `window.KPApi.getKbeautyProducts(q,lang)` 호출 → 상품 0개면 `sec.remove()`+`.kp-topads:empty{display:none}` 로 **자동 숨김**(현재 `ALI_APP_KEY`/`ALI_APP_SECRET` 시크릿 미설정이라 휴면; 키 넣으면 자동 활성). **롤아웃 범위:** 생성 SEO 311페이지 전부(build-seo.cjs `shell()` 에 컨테이너+config/api-client/topads 스크립트, seo-ad-top 이미 있어 `data-ads="off"`) + 콘텐츠 허브(festivals·culture·temples·nightviews, `.kp-ad-top` 다음 주입) + index.html(상단 AdSense 없던 페이지라 `data-ads` 미지정=풀스트립, categories 섹션 앞) + kpop/kbeauty 버티컬. **유틸/법무 페이지(about·privacy·terms·contact·currency·etiquette·kdrama·menu-translator·phrases·seasons·subway)는 의도적 제외**(thin page AdSense 정책 + 휴면 가치 낮음). 향후 추가시 api-client+WORKER_URL+topads 와이어링 필요.

**⚠️⚠️ 배포 핵심 교훈(2026-06-18): 개별 scp 가 비신뢰 → tarball 단일전송 사용.** `deploy-to-guide.ps1` 의 파일별 scp 가 **루트파일+초반 SEO디렉터리(places/guide/itinerary/faq/blog)는 조용히 실패하고 후반(ja/zh/es/sitemap)만 성공**하는 부분배포가 재발(원인 미상, scp 연결 플레이크 추정; PowerShell 은 scp 실패해도 멈추지 않음). 게다가 `Tee-Object` 가 출력을 버퍼링해 진행상황이 로그에 안 보임. **해결법(검증됨):** ①`ssh ... "sudo chown -R bitnami:daemon ... && chmod -R ug+rw ..."` 사전권한 ②로컬에서 배포대상(deploy-to-guide.ps1 의 ROOT/MODULE/ASSET/MESSAGE/ICON 배열 + SEO디렉터리 places·guide·itinerary·faq·blog·ja·zh·es + explore.html·llms.txt·kp*.txt)을 매니페스트로 만들어 `tar -czf deploy.tgz -T manifest` ③`scp deploy.tgz /tmp/` **단 1회 전송** ④`ssh "cd /guide && tar -xzf /tmp/deploy.tgz && sudo chown -R bitnami:daemon . && find -type d -exec chmod 755 + && find -type f -exec chmod 644 + && rm /tmp/deploy.tgz"`. **주의: GNU tar 추출은 mtime 보존** → 안 바꾼 파일은 옛 mtime 유지(정상, 내용은 최신). 라이브 검증은 `curl 'https://koreaplus-lifes.com/guide/<path>?cb=$RANDOM'`(CF HTML 캐시 안 함). 311 SEO + 허브 + kbeauty.js 쿠팡0 전부 라이브 확인 완료.

**#6 스트리밍 허브 완성(2026-06-18, 라이브 kp-v36):** Top-10 로드맵 #6(키 불필요) 구축 완료. `kpop.html` 에 필터칩(`data-target="kpop-streaming"`)+섹션(`#kpop-streaming`>`#kpop-stream-body`) 추가, `modules/kpop.js` 에 `STREAM_STR`(9개언어)+`STREAM_PLATFORMS`(Spotify/Apple Music/YouTube Music/Melon 공식검색 링크)+`renderStreaming()`+`renderChecklist()` 추가(boot()에서 renderArtists 다음 호출), `kpop.css` 에 `.stream-*` 스타일. **핵심 기능 = 데일리 스트리밍 체크리스트**(5개 미션, localStorage `kp_stream_check`={date,done[5],streak,last}, KST 자정 리셋, 진행바, **🔥 연속 스트릭**). 투표는 **의도적 미구현**(KV 무료티어 쓰기 1000/일+1write/s/key 제약, 어뷰징/레이스 → 부적합). 칩/섹션제목·부제는 messages/*.json 3키(`kpop.filter.streaming`,`kpop.sec.streaming.title`,`.sub`), 카드/체크리스트 내용은 STREAM_STR(=메시지JSON 안 건드림). **스트릭 단복수**: STREAM_STR.streak 값에 `단수형|복수형` 파이프 인코딩(en/ko/ja/zh/id는 불변→파이프없음, es/fr/de/pt만), 렌더시 `n===1?단수:복수`(day1이 최빈 상태라 중요). **배포 전 교차검증 워크플로**(언어별 네이티브 8 + 코드 1) 돌려 zh(打歌→打榜)·es(título→tema principal)·de(Serie→Tage in Folge 등 4건)·pt·id 수정 + 코드 HIGH버그(장시간 탭 KST자정 넘김시 스트릭 손상 → 체인지핸들러에 날짜 재도출 가드)+progressbar aria-label/valuetext 반영. 굿즈 스트립은 이번에 kdrama-locations·seasons 에도 추가(이제 전 콘텐츠 페이지 커버). 검증: preview에서 렌더·체크리스트·스트릭 연속성(de 1Tag→2Tage, es 1día)·en/ko/de/es 현지화·필터격리·콘솔0, 라이브 curl 전부 통과.

**✅ AliExpress 제휴 라이브 활성화(2026-06-18):** 사용자가 콘솔에서 `ALI_APP_KEY`=536770, `ALI_APP_SECRET`=`LZsv…y9Qc` 확인 → `wrangler secret put` 으로 워커에 설정(둘 다 `secret list` 확인됨). **⚠️ 핵심 함정: tracking_id.** `src/api/aliexpress.ts` 의 `TRACKING_ID` 가 `'koreaplus'` 였는데 그 제휴계정엔 미등록 → API `resp_code 402 "TrackingId input parameter error"`(서명·키는 정상, HTTP 200). **해결: `'luckynum'` 으로 변경**(lucky-webapp worker-seo-template.js:1110 에서 쓰는, 같은 536770 계정에 등록된 tracking_id). 변경 후 즉시 상품 반환(kpop 2·kbeauty 9·여행 12, ₩geo-로컬). 사용자 PowerShell 실행정책(`UnauthorizedAccess` npx.ps1 차단)은 `npx.cmd` 또는 `Set-ExecutionPolicy -Scope Process Bypass` 로 우회. 디버그법: `aliexpress.ts.searchProducts` 가 에러를 []로 삼켜서, `?debug=ali` 임시 라우트(raw resp 반환, app_key/sign 마스킹)로 진단 후 제거. **withCache 키에 `v2` 버전 넣음**(tracking 고치기 전 캐시된 빈 결과 12h 잔존 방지). 워커는 로컬 `npx wrangler deploy` 로 배포(CI는 KV write 권한 없음). **참고: 현재 koreaplus 매출이 lucky 와 같은 'luckynum' tracking_id 로 귀속**(어트리뷰션 혼재) → 분리하려면 제휴콘솔에서 'koreaplus' tracking ID 생성 후 TRACKING_ID 교체. 워커 라우트 `/api/kbeauty/products` 가 키 있으면 실제 호출(router.ts:464), 없으면 [].

**검색 트래픽 폭발 프로그램(2026-06-18~):** 현 SEO 상태는 이미 강함 — hreflang(HTML)·og:locale(9)·JSON-LD(Article/FAQ/Breadcrumb/ItemList/Org/WebSite/Speakable, 이 콘텐츠 타입에 맞는 정확한 세트)·E-E-A-T 바이라인(trustBlock)·CWV preconnect/defer/font-display·내부링크클러스터(cityClusterLinks)·CTR 타이틀("(2026)")·PWA. **즉 SEO 펀더멘털 ~8/10은 이미 구현됨** → HowTo(구글 2023 폐기)·Event/Video/Recipe(데이터 없음) 추가는 무효마크업 위험이라 안 함. **Wave1 완료·라이브(2026-06-18):** ①**사이트맵 hreflang `<xhtml:link>` 주석** — `build-seo.cjs` 의 `shell()` 이 alts 있으면 `HREFLANG_SM` Map 에 클러스터 적재 → `urlEntry` 가 `xhtml:link`(+x-default) 방출, `urlset` 에 `xmlns:xhtml`. 라이브 1110개 주석 검증. ②**IndexNow 자동제출**(Bing/Naver/Yandex 즉시색인 — Naver=한국 1위라 핵심) — `indexnow-submit.cjs`(sitemap의 `/guide/` URL만 추출해 POST, 키가 `/guide/`에 있어 비-/guide URL(`/kpop`,`?lang=`)은 제외해야 함 — 안 그러면 422), `deploy-to-guide.ps1` 끝에 `node indexnow-submit.cjs` 추가. 385 URL 제출 HTTP 200 확인. **⚠️ #1 미완 레버 = 다국어 확장: `LOCALES=['ja','zh','es']` 뿐, 5개 언어(ko=Naver 최대시장·fr·de·pt·id) 미생성.** 확장은 콘텐츠테이블(CITY_L10N/ITIN_L10N/BLOG_L10N/FAQ_L10N + month/visa/cost 번역 + CITY_FACTS/CITY_NAME_L10N + L10N dir맵) 전부 채워야 함. **주의: buildMonthL10n/buildVisaL10n/buildCostIndex 는 LOCALES 를 무조건 순회** → LOCALES 에 'ko' 추가하면 번역없는 월/비자/비용 페이지가 깨진 채 생성될 위험 → 언어 추가는 콘텐츠 먼저 채우고 해야 함(워크플로 번역생성+검증 권장).

**✅ 다국어 SEO 확장 완료·라이브(2026-06-20): ko·fr·de·pt·id 5개 언어 추가.** `LOCALES` 가 `['ja','zh','es']`→8개로 확장. 워크플로(언어별 생성+네이티브 검증)로 각 언어의 **L10N 엔트리**(12개월 months + tw UI + 완역 visa) 생성 → `seo-l10n-extra.js`(빌드 입력, 서버 미배포)에 함수형 q1/q2/lead(`{m}` 치환)+엔티티 디코드(`&lt;`→`<`)+말미마침표 제거로 기록 → build-seo.cjs 에서 `Object.assign(L10N, require('./seo-l10n-extra.js'))`. **⚠️ 함정(해결): 이 require 는 기존 `Object.assign(L10N,{ko:{dir,visa:{h1}}...})` 미니스텁(city-guide 용, line 306~) *뒤에* 와야 함** — 앞에 두면 스텁이 months/tw 를 덮어써 `L.months[idx]` undefined 크래시. 빌더 수정: buildMonthL10n 의 h1(8개언어 맵)·moreH 맵·`dot`(ja/zh=`。`, 그외=`. ` — 라틴/한글 마침표; 기존 es도 `。`였던 것 교정)·buildVisaL10n bestH 맵 모두 5개 언어 추가, buildCostIndex hreflang 은 `LOCALES.filter(l=>COST_L10N[l])` 로 가드(cost 는 ja/zh/es만 있어 깨진 alt 방지), explore FLAGS+제목 8개언어. **결과: l10n month+visa 39→104(+65), 총 461페이지, 사이트맵 hreflang 1110→2440.** ko `3월 한국 여행`·fr `La Corée en avril`·de title 네이티브, `。`0, `<strong>`디코드, lang속성·hreflang10 전부 라이브 검증. deploy-to-guide.ps1 SEO_DIRS 와 tarball manifest 에 ko/fr/de/pt/id 추가함. **✅ FAQ 5개 언어 확장 완료·라이브(2026-06-20):** `FAQ_L10N` 에 ko/fr/de/pt/id 추가(각 ui 7개 + items 20개 Q&A). 워크플로(생성+네이티브 검증, 20 FAQ×5) → `seo-faq-l10n.js`(엔티티 디코드, items 배열→`{slug:[q,a,body]}` 키객체) → build-seo.cjs FAQ_L10N 정의 직후(line~1774) `Object.assign(FAQ_L10N, require('./seo-faq-l10n.js'))`. **+100 페이지, 총 561, 사이트맵 589 URL, IndexNow 580 제출.** ko/fr/de/pt/id FAQ 라이브 검증(h1 네이티브·`<p>`디코드·FAQ JSON-LD·hreflang10·lang속성), EN FAQ 도 8개언어 hreflang 으로 갱신. 패턴: 신규 언어셋은 `seo-*-l10n.js` 생성파일 + Object.assign 머지(빌드 입력, 서버 미배포). **✅ COST_L10N 5개 언어 완료·라이브(2026-06-20):** `seo-cost-l10n.js`(생성+검증, COST_L10N 정의 직후 Object.assign 머지). 비용지수 페이지가 `{lang}/guide/korea-travel-cost-index.html` 에 5개 추가(566p). ⚠️ 워크플로 verify:id 가 일시 rate-limit 으로 빠졌으나 **resume(resumeFromRunId)로 ko/fr/de/pt 캐시 + id만 재생성**해 5개 완성 — resume 패턴 유효. IndexNow 587 제출. **✅ BLOG·ITINERARY 5개 언어 완료·라이브(2026-06-20) — 다국어 확장 프로그램 전체 완성.** BLOG_L10N(blog-l10n.json, 5글×5=25)·ITIN_L10N(itinerary-l10n.json, 10일정×5=50) 에 ko/fr/de/pt/id 병합. **⚠️ BLOG/ITIN 은 `.js` extra파일이 아니라 기존 JSON(blog-l10n.json/itinerary-l10n.json)에 직접 병합**(build-seo 가 OUT/에서 readFileSync) — Object.assign 불필요. **⚠️ BLOG_UI·ITIN_UI(faqH/moreH/ctaH/ctaP/planBtn/enBtn/badge 등) 맵도 5개 언어 인라인 추가 필수**(없으면 `ui.faqH` undefined 크래시). **⚠️ 일정 워크플로 교훈: parallel(10) 동시실행 → 서버 burst rate-limit(또는 세션한도) 반복 실패 → 순차 `for…await agent()` 1개씩 실행으로 해결**(45분 걸렸지만 완주). itinerary 구조=`{h1,title,metaDesc,lead,days:[{label,plan}],tips:[],faq:[{q,a}]}`. **최종: 641페이지(311→641), 사이트맵 674 URL, IndexNow 665 제출. months·visa·city·FAQ·cost·blog·itinerary 전 콘텐츠타입 × ko/fr/de/pt/id 전부 라이브 검증완료**(h1 네이티브·`。`0·HTML디코드·hreflang10·lang속성·EN reciprocal). 생성파일: seo-l10n-extra.js·seo-faq-l10n.js·seo-cost-l10n.js(빌드입력) + blog-l10n.json·itinerary-l10n.json(직접병합). 워크플로 재개는 resumeFromRunId(완료에이전트 캐시).

**✅ 다국어 품질 QA 완료·라이브(2026-06-20):** 시스템 스캔(전 440개 ko/fr/de/pt/id/ja/zh/es 페이지) = **0건**(엔티티leak·undefined·플레이스홀더·mojibake·CJK마침표·빈h1/title·lang불일치 + hreflang 깨진alt/비상호 전부 0). 언어 QA(언어별 네이티브 검수 워크플로, 순차) = 5개언어 4~5/5점, **23건 전부 미검수 itinerary 에 집중**(months/visa/faq/cost/blog 는 생성시 검수돼 클린). 수정: ko 흥례문→광화문(경복궁 정문/수문장교대식 위치 팩트), de `가-galbi`→`닭갈비`(깨진한글), pt `magazines`→`lojas de departamento`(false cognate)·anglicism, fr Séoulites→Séouliens/buskers→artistes de rue/concert 단복수, id 자연스러움+안압지→동궁과 월지. 적용=`itinerary-l10n.json` itin[*][lang] 서브트리 재귀 string-replace(current→suggested, 23/23 적용)→재생성→배포. 검토번들 추출: `C:/tmp/review-<lang>.json`(seo-l10n-extra.js+seo-faq/cost-l10n.js+blog/itinerary-l10n.json 에서 언어별 합본; 대용량 소스 직독 회피). 교훈: itinerary 만 generate-only(검수無)였던 게 오류 원천 → 향후 generate+verify 권장.

**🆕 K-pop 역사 SEO 섹션 — 1,500+페이지 확장 시작(2026-06-20, Batch 1 라이브):** 목표=641→1,650+(K-pop史 ~108유닛×9언어). **아키텍처: `buildKpopHistory(slug,lang)` 생성기**(build-seo.cjs, RUN+sitemap 통합) — 콘텐츠는 `kpop-history.json`(`{slug:{en,ko,ja,zh,es,fr,de,pt,id:{h1,title,metaDesc,lead,sections:[{h,body}],faq:[{q,a}]}}}`)에서 읽음, URL=`kpop/<slug>.html`(EN)+`<lang>/kpop/<slug>.html`, `<base href=/guide/>` 라 내부링크는 `kpop.html`/`${dir}kpop/<s>.html`, hreflang 9개 클러스터 자동, Article+FAQ+Breadcrumb. `KPOP_HIST_LANGS`(9)·`KPOP_HIST_EMOJI`·`KPOP_HIST_MORE`(섹션제목 9언어) 맵. deploy SEO_DIRS+manifest 에 `kpop` 추가. **Batch 1 완료=10기둥토픽**(kpop-history 개요+1~5세대+SM/YG/JYP/HYBE史)**×9=90페이지 라이브(→731).** 파이프라인: ①영문작성(워크플로, 엄격 정확성룰)→②사실검증(독립 3검수자+웹서치, TXT 'first boy group since BTS' 1건만 수정)→③8언어 번역(순차 16배치=8lang×5토픽half, burst회피)→④시스템스캔(90p 0건)→⑤언어QA(순차 8lang, 전부 9/10, 19건 수정: ko '빅뱅'오역→'모든 것의 시작'·ja '2025'→'2026'·zh/ja KWANGYA 旷野/クァンヤ·es 'importó por'직역 등)→배포+IndexNow. **교훈: 번역 워크플로에 verify 스테이지 없으면 언어QA 필수. 사실은 전 언어 정확 보존됨.** **✅ B2(전설 24팀) 라이브(2026-06-20):** EN작성→사실검증(독립3, 0건)→8언어번역(192)→시스템(306p 0건)→언어QA(8lang 8~9점, 34건+α 수정). **⚠️ QA 샘플이 반복 calque 를 전수 못 잡음**(예: fr 'transgenre'=genre-bending 오역이 5회 중 2회만 플래그) → **명백한 반복 오역은 언어별 global string-replace 로 일괄수정**(transgenre→métissé, ja 言い過ぎ calque 등). zh H.O.T. 'Tony Ahn 본명 안승호'를 강타 본명으로 오역·Highlight '6명을 5명'·SuperJunior 데뷔년도 drift 등 번역 사실드리프트도 잡음. EN highlight lead 'five(then four)'→'six(later four)' 내부정합 수정. **현재 ~947페이지(B1 90+B2 216), 사이트맵 980, IndexNow 971.** 진행중: B3 용어16+B4 마일스톤12+B5 테마8=36토픽 통합 작성. 잔여: B6 로스터34. 목표 ~1,577. **✅ 독립 K-pop 사이트맵 재구성(2026-06-20):** 기존 `kpop-sitemap.xml`(도메인 루트 `/opt/bitnami/wordpress/kpop-sitemap.xml`, /guide/ 아님!)이 수작업 3 URL 뿐이었음. build-seo.cjs 에 생성기 추가(메인 sitemap.xml 직후) — `/kpop` 허브 + out.kpopHist 전부 + kpop테마 페이지(out.categories/itineraries/itinL10n/places 에서 /k-?pop/ 필터) → `OUT/kpop-sitemap.xml`(320 URL, hreflang 3150, urlEntry 재사용). **루트 배포 필수**(scp → `$WP_ROOT/kpop-sitemap.xml`, WP_ROOT=REMOTE_DIR -replace /guide$); deploy-to-guide.ps1 에 단계 추가함. 라이브 robots.txt 가 이미 참조. 배치 추가 시 재생성+루트재배포 필요. (참고: Google/Bing sitemap ping 엔드포인트는 2023 폐기—410/404 정상, robots.txt+GSC로 발견).

**핵심 함정:** ①nav 3중 복제(build-seo.cjs PRIMARY_NAV + index.html + 각 허브페이지 인라인 kp-pnav) — 항상 3곳 동시수정. ②AdSense 저작권: 소속사 사진 핫링크/호스팅 금지(자체 데이터시각화+텍스트만, 뉴스는 제목+발췌+링크). ③withCache KV 최소 TTL 60s. ④tsc --noEmit 게이트 — 새 src/api/*.ts·schema 인터페이스 타입클린 필수, WorkerEnv 신규키는 `?:` 옵셔널. 관련: [[lucky-deploy-pipeline]].
