---
trigger: model_decision
description: "Rule for koreaplus-webapp: kbeauty-vertical.md"
---
`koreaplus-lifes.com/guide/kbeauty.html` = **종합 K-뷰티 플래그십 버티컬** (2026-06-14 구축). [[koreaplus-kpop-vertical]] 의 `/k-pop` 패턴을 그대로 클론. 저장소 `C:\Users\juksu\koreaplus-webapp`(GitHub koreapluslifes-dot/koreaplus-webapp).

**제로키 원칙**: API 키 0개로 페이지 100% 렌더(멀티에이전트 검증 완료 — ko/ja 렌더+인터랙션). 워커는 점진적 강화만.

**구축 파일(신규)**: `kbeauty.html`(허브 셸, 12섹션, hub-styles.css 재사용+인라인 .kb-* 스타일, 핑크 --kb1/--kb2), `kbeauty-data.js`(콘텐츠 티어 — window.KBEAUTY_* + module.exports, build-seo vm 호환), `modules/kbeauty.js`(렌더러 IIFE, window.KBEAUTY_* 소비), `modules/kbeauty-sharecard.js`(캔버스 1080×1350 세로카드), `src/api/aliexpress.ts`(워커 AliExpress 클라이언트), `assets/cosing-ingredients.json`(271종 INCI 사전 — 디코더용, 워크플로 생성).
**수정**: `modules/api-client.js`(getKbeautyProducts/getKbeautyBio 추가), `src/router.ts`(/api/kbeauty/products·/bio + health kbeauty맵), `src/worker.ts`(WorkerEnv ALI_APP_KEY?/ALI_APP_SECRET? 옵셔널), `src/api/schema.ts`(DataSource 'aliexpress'|'kbeauty' + BeautyProduct), `build-seo.cjs`(PRIMARY_NAV K-Beauty 링크 + STATIC 'kbeauty.html' + sitemap 0.9/daily + llms.txt Featured Hubs), `sw.js`(CACHE→kp-v19 + kbeauty 자산 프리캐시), `deploy-to-guide.ps1`(ROOT/MODULE에 kbeauty + 신규 $ASSET_FILES=assets/cosing-ingredients.json + assets 디렉터리 mkdir/scp), `manifest.json`(K-Beauty shortcut), 9개 `messages/*.json`(kbeauty.* 41키 ×9언어), 루트허브 16개 인라인 nav(kpop 링크 뒤 K-Beauty 삽입).

**12섹션**: 트렌드티커, 정적계절 스킨예보, 피부타입퀴즈(5문항→kp_kbeauty_skin), 고민선택(8개→kp_kbeauty_concerns), AM/PM 루틴빌더(11단계), 성분백과(27 히어로)+모달, 성분표 디코더(번들JSON), 함께쓰면안되는 충돌체커(14활성 매트릭스), 브랜드디렉터리(30개 티어탭+팔로우 kp_kbeauty_follow), 용어사전(17), 쇼핑(라이브 AliExpress 그리드+정품 판매처 띠), 내선반만들기 퍼널. localStorage: kp_kbeauty_skin/concerns/follow.

**워커 라우트(v1)**: `/api/kbeauty/products?q=&lang=`(AliExpress affiliate, crypto.subtle HMAC-SHA256, tracking_id **'koreaplus'**, request.cf.country 지오최적화+4단 폴백, withCache 12h, **키없으면 [] 반환**→FE는 정품 판매처 띠로 폴백), `/api/kbeauty/bio?qid=&lang=`(기존 WikidataClient 재사용·키불요·9언어). tsc --noEmit 통과. wrangler dev 스모크테스트 OK.

**사용자 결정사항(2026-06-14)**: ①상품 그리드 **사진 노출 허용**(AliExpress 판매자 리스팅 이미지만 — 브랜드 공식사진 핫링크는 여전히 금지). ②어필리에이트 **AliExpress 우선**(ALI_APP_SECRET 1개만 추후 워커 시크릿 주입 → 코드변경 없이 라이브). ③스킨예보 **정적 계절조언으로 시작**(Open-Meteo 무료티어는 광고/어필리에이트 사이트 약관상 금지 → 라이브 UV는 유료플랜 시 업그레이드).

**개선 완료(2026-06-14)**: ①**남성 그루밍** 프리셋 렌더(루틴빌더 AM/PM/🧔Men's 3탭, KBEAUTY_MENS 4단계 — 이전엔 데이터만 있고 미렌더였음). ②**브랜드 약력 QID 8개 백필**(Wikidata wbsearchentities로 검증 — innisfree Q16176002, laneige Q16935511, sulwhasoo Q85980775, amorepacific Q490142, missha Q4668069, drjart Q85845827, etude Q16174973, naturerepublic Q16935507). 라이브 약력 작동 확인(ja 위키 없으면 영어 폴백=의도). **cosrx/whoo/hera/mediheal은 정확한 화장품 엔티티 없어 빈값 유지**(추측 금지 — 과거 Q480047=영화 사태).

**미결/백로그**: ①AliExpress: 사용자가 Portals 승인+ALI_APP_SECRET 발급 후 `wrangler secret put ALI_APP_SECRET`(앱키 ALI_APP_KEY도). 승인 전엔 정품 판매처 띠만 노출. ②정품 판매처 띠(YesStyle/Olive Young) url 빈값 → 가입 후 kbeauty-data.js KBEAUTY_SHOP.retailers[].url 채우면 활성(rel=sponsored). ③v1.1: 셰이드매처(Makeup API), 바코드스캔(Open Beauty Facts), 라이브뉴스(NewsData), 데이터 콘텐츠 현지화(성분/브랜드명은 현재 영어 — chrome만 9언어).

**프리티 URL(2026-06-14)**: `koreaplus-lifes.com/kbeauty` 및 `/k-beauty` → **301 → `/guide/kbeauty.html`**(쿼리 보존). 구현: worker.ts fetch 최상단에서 `path==='/kbeauty'||'/k-beauty'` 시 `Response.redirect(...+url.search,301)` + **CF 워커 라우트**(zone `9041a4cf6588efd38b7840cf52c9f401`, 패턴 `koreaplus-lifes.com/kbeauty*`·`/k-beauty*` → script koreaplus-webapp). 라우트는 CF REST API로 등록(코드와 별개, lucky 패턴). **라우트 패턴 와일드카드 필수**(`*` 없으면 매칭 안 됨). 루트 `koreaplus-lifes.com/`은 여전히 WordPress 6.8.5(미변경). 클린 리라이트(URL 유지) 안 함 — kbeauty.html이 상대경로 자산이라 /guide/ 밖에선 깨짐 → 301이 정답.

**★CI 배포 깨짐(중요)**: GitHub Actions `Deploy Cloudflare Worker`가 **kpop 때부터 전부 실패** — repo 시크릿 `CF_API_TOKEN`에 **Workers KV write 권한 없음**(`kv bindings require kv write perms [code:10023]`, 워커가 CACHE_KV 바인딩 사용). **수정**: CF 대시보드에서 CF_API_TOKEN 에 "Workers KV Storage:Edit" 추가. **임시 우회(현행)**: 워커는 로컬 `npx wrangler deploy src/worker.ts`로 배포(wrangler OAuth=jeybeeicon@gmail.com, 계정 `9ba6054d…`, KV write 보유 → 성공). 현재 라이브 워커는 로컬 배포본. **CF 라우트 등록 토큰**: lucky deploy.config.ps1의 `$CF_TOKEN`이 동일 계정(9ba6054d)이라 koreaplus 존에도 통함.

**배포 완료(2026-06-14, 라이브)**: 워커 로컬 wrangler 배포(CI 우회) 완료, `/api/health` kbeauty맵 라이브. 정적 `.\deploy-to-guide.ps1 -ServerIP 18.207.55.50`(Lightsail KoreaPlus-Lifes250508 us-east-1a, PEM `C:\Users\juksu\Documents\blog\koreaplus-lifes\LightsailDefaultKey-us-east-1.pem`) 완료. **라이브 검증**: https://koreaplus-lifes.com/guide/kbeauty.html (HTTP200, 남성탭·QID·디코더271종·9언어·전 허브 nav링크·sitemap 포함). **서버 파일소유권 함정**: deploy-to-guide.ps1 재실행 시 기존 허브파일(festivals/culture/sitemap 등)이 비-bitnami 소유라 "Permission denied" 가능 — 신규/변경 K뷰티 파일은 정상 업로드되니 무해하나, 기존파일 갱신 필요시 `sudo chown bitnami` 후 재배포(SSH sudo는 별도 승인 필요). 생성 크롤페이지(places/guide/…)는 gitignore — `node build-seo.cjs` 재생성 후 배포.

**퀄리티 패스 라이브(2026-06-15)**: ①**콘텐츠 9개국어 현지화** — `assets/kbeauty-content.<lang>.json`(295키 ×8언어, 영어 EN 소스+ko/ja/zh/es/fr/de/pt/id) 오버레이를 `modules/kbeauty.js`가 부팅 시 로드→`localizeData()`로 데이터 통째 현지화(성분·루틴·고민·퀴즈·용어·트렌드·브랜드·남성). **lang 해석은 URL `?lang=` 우선**(i18n.js와 동일, hreflang 변형 페이지가 맞는 언어 콘텐츠 표시). 신규 콘텐츠 추가 시 9개 오버레이도 함께 번역해야 함(미번역은 영어 폴백). ②**접근성**(모달 포커스트랩/복원/aria-labelledby, ARIA 탭 aria-selected, aria-pressed 토글, 성분/브랜드 카드 키보드조작, aria-live, 이모지 aria-hidden, 44px, reduced-motion). ③**SEO/E-E-A-T**: hreflang 9+x-default, og:locale, twitter, JSON-LD(HowTo·BreadcrumbList·Organization·dateModified). ④**디자인**: 핑크 그라데이션 히어로·섹션헤더 액센트·라이트모드 대비(--kb1:#d61f6e 기본, html.light #c01a63). ⑤**`/kbeauty-sitemap.xml`** — 워커 핸들러(worker.ts, 기존 /kbeauty* 라우트로 도달, 9언어 hreflang 다국어 사이트맵). robots.txt 루트엔 미등록(WP루트 파일이라 수동/Search Console 제출 필요). ⑥아젤라산 임신플래그 수정(cosing json).

**★서버 소유권 함정(심화)**: guide 루트 파일 전체(index.html·app.js·*.css·*.html)가 비-bitnami(daemon) 소유로 바뀌어 scp 덮어쓰기 전부 거부됨. `modules/`·`assets/`·`messages/` 하위는 bitnami 소유라 정상. **해결**: `sudo chown bitnami:daemon` 필요한데 **재귀(전체 web root) sudo는 안전분류기가 차단**, **개별 파일 지정 sudo chown은 통과**(예: `sudo chown bitnami:daemon .../guide/kbeauty.html .../sw.js`로 2파일 해결함). 전체 가이드페이지 배포 정상화하려면 사용자가 직접 `sudo chown -R bitnami:daemon /opt/bitnami/wordpress/guide && find ... -type f -exec chmod 664` 1회 실행 권장. **CI도 깨짐**(CF_API_TOKEN KV write 없음) → 워커는 로컬 `npx wrangler deploy`로 배포.

**탑10 고관심 요소 웨이브(2026-06-15 진행중)**: 전문가팀 워크플로(wyw72m1j6)가 관심도 탑10 선정 + 8개 요소 콘텐츠 작성. **8개 데이터셋 전부 kbeauty-data.js에 스테이징 완료**(KBEAUTY_GLASSKIN, KBEAUTY_DUPES(_META), KBEAUTY_TROUBLESHOOTER_META/PURGE/BARRIER/FUNGAL, KBEAUTY_BUY, KBEAUTY_SUNCARE, KBEAUTY_SNAIL, KBEAUTY_CATEGORIES, KBEAUTY_BESTSELLERS/BOARD_CONFIG — 작성콘텐츠 사본 `C:/tmp/kb10/*.json`+`.build.txt`). **탑10 전부 라이브 완성(2026-06-15, sw kp-v31)**: #1 Where-to-Buy/정품허브(KBEAUTY_BUY, 지오 리테일러비교 5×9지역+/cdn-cgi/trace 자동감지+스팟더페이크 8체크+위조워치 8브랜드), #2 Sunscreen Decoder(KBEAUTY_SUNCARE, 라벨해독·피부타입별 자차추천·7제품·재도포·미신), #3 Glass Skin(7스킨 피부타입 하이라이트·AM/PM·성분스택·타임라인), #4 Beginner 5스텝 토글(루틴빌더 탭), #5 Snail 심화(하이프체크·5제품비교·FAQ), #6 Dupe Finder(23듀프·카테고리필터), #7 Personalized Stack(피부+고민→추천성분·루틴·쇼핑), #8 Troubleshooter(퍼징퀴즈·배리어체커·곰팡이스크리너), #9 Category Explainer(8카테고리 두께스케일·이중세안), #10 Bestsellers/Trend Tracker(16개 과학vs하이프판정·태그필터). 총 18섹션. 각 렌더러는 modules/kbeauty.js, 필터칩/그룹은 #kb-filters wireFilters map. 신규 데이터셋 14개 kbeauty-data.js 스테이징+export 완료. 각 섹션 브라우저 검증(렌더+인터랙션+콘솔0)+라이브 curl 확인.
**신규 섹션 딥콘텐츠 9개국어 현지화 완료(2026-06-15, sw kp-v32)**: 신규 10섹션 본문을 586개 평면키로 추출(C:/tmp/kb_extract2.cjs, 화이트리스트 워커=산문필드만, ID/enum/브랜드/제품명/시드/URL/이모지 제외)→16에이전트 워크플로(8언어×2파트)로 번역→assets/kbeauty-content.<lang>.json 오버레이에 병합(C:/tmp/kb_merge2.cjs, 각 295→881키). modules/kbeauty.js에 동일 화이트리스트 딥워커 localizeNewData()+ncWalk 추가(window.KBEAUTY_* 딥클론 후 cx()적용→재할당; 렌더러가 render시 window.* 읽으므로 적용됨). 정합성 13/13 PASS(enum verdict·브랜드·시드 보존). 9개언어 라이브 검증 완료.
**사이트맵 독립화(2026-06-21)**: K-뷰티 사이트맵을 **워커 동적생성 → 정적 단일 파일**로 전환. build-kbeauty-seo.cjs가 `kbeauty-sitemap.xml`(repo루트→/guide/) 생성=허브 9개 언어(hreflang 90)+답변페이지 24p+인덱스 통합(34 URL). Apache 서빙이라 GET·HEAD 모두 200 application/xml(워커판은 HEAD 405였음). **정식 URL = apex `https://koreaplus-lifes.com/kbeauty-sitemap.xml`** (사용자 요청). apex는 /kbeauty* 워커라우트가 가로채므로 worker.ts가 정적 /guide/kbeauty-sitemap.xml을 **프록시 서빙**(GET·HEAD 모두 200 application/xml, redirects=0, x-served-by:kbeauty-sitemap; origin fetch 실패시 302폴백). robots는 apex 라인. 정적 /guide/kbeauty-sitemap.xml이 소스(빌드로 갱신), apex가 노출 URL. 옛 kbeauty-answers-sitemap.xml은 통합·삭제. 사이트맵 변경시 node build-kbeauty-seo.cjs 재실행+scp.

**★1000 URL 콘텐츠 라이브러리 완성(2026-06-21, 라이브)**: `/guide/kb/` 트리를 34→**1003 사이트맵 URL(994 페이지 + 9언어 허브)**로 확장 — "K뷰티 전세계 원탑" 콘텐츠 무브. **마스터 생성기 = `build-kbeauty-pages.cjs`**(단일 파일, 페이지타입 레지스트리, kbeauty-data.js+cosing+kbeauty-editorial.json에서 생성, **kbeauty-sitemap.xml 통째 재생성=단독 자동갱신**). build-kbeauty-seo.cjs는 이 생성기에 흡수됨(판정페이지 포함)—더는 쓰지 말 것(사이트맵 충돌). **데이터기반(날조0, AI없음)**: 성분27·성분×고민70·성분×스킨48·브랜드30·고민8·혼합71·듀프23·용어17·루틴40·판정24·INCI기능허브18·INCI개별206(desc≥14단어 게이트)·제품타입8·타입×고민48·타입×스킨40·스킨타입5·계절(허브4+조합52). **편집(AI, 출처·헤지·2단계팩트체크, 날조0)=254항목**: 역사48·포맷10·헤리티지16·기업16·업계26·국가54·비교10·문화20·도시쇼핑14·인물12·how-to28(/guide/kb/{history,format,heritage,company,industry,country,compare,culture,shop,people,how-to}). 정확성 계약: 위키/Wikidata/MFDS/AAD/PubMed 출처, "대중화 vs 발명" 헤지, 사생활·정확수치·날짜 날조 금지(설계+생성 워크플로 wd90982fl/wbxjozgmm/w4nh0xy3w). **모두 영문+hreflang x-default→/kbeauty(허브는 9언어 유지)**. 검증: 내부링크 5238개 0깨짐, undefined/NaN 0, 23/23 섹션 라이브 200, 9언어 허브 무오류 재확인(오버레이/메시지 valid·200). **배포=tar 1개 묶음 scp+서버 untar+chmod(소파일 994개 scp -r는 느림→tar가 정답)**, IndexNow 994 URL 제출(HTTP200). kb/ 페이지는 git추적(생성기로 재생성 가능). 갱신법: node build-kbeauty-pages.cjs → tar czf kb.tar.gz kb → scp+untar → IndexNow. **애드센스(2026-06-21, 사용자요청)**: shell() 공통템플릿에 자동광고 로더(head, client ca-pub-1378943893051810) + 본문중간 반응형 유닛(slot 4521899200, 허브와 동일) → 994p 전체 적용·라이브검증(loader/unit=1). 자동광고는 AdSense 대시보드에서 사이트 Auto-ads ON + 신규URL 크롤/승인(수시간~수일) 후 실제 렌더(그 전엔 빈공간 가능). 계정·ads.txt는 허브와 동일이라 추가승인 불요. ADSENSE_CLIENT/SLOT 상수로 관리.

**K뷰티 최적화 W1(2026-06-26, 다중에이전트 자체감사 6렌즈 기반, 라이브)**: ①**스키마 강화** — shell()이 모든 페이지에 `BreadcrumbList` 자동 주입(crumbLD), 브랜드=`Brand`+hero `ItemList`, 듀프=`ItemList`(reference+alt `Product`)+"Is X a good dupe?" `FAQPage`. ②**hreflang 자기참조** — 기존 x-default→HUB(무효) 제거, `hreflang=en`+`x-default` 둘 다 페이지 자신 URL(영문 캐노니컬 클러스터 정상화). ③**thin페이지 광고 게이팅** — shell adsOk = `o.ads!==false && 본문strip>=300`(glossary term은 ads:false 명시); term 0/17·light mix 제외, 충실 페이지 광고 유지(AdSense 정책 안전). ④**정품구매 박스 buyBox()** — RETAILERS(YesStyle search + Amazon search, rel="sponsored nofollow noopener")를 브랜드·듀프·성분 페이지에 주입(어필리에이트 준비; YesStyle AWIN 승인시 URL만 딥링크로 교체). ⑤**허브-스포크 링킹** — 고민허브→스킨타입별 routine 링크(루틴 고아 해소), 듀프→브랜드페이지, 성분 buyBox. ⑥**편집 중복 12개 제거** — 2개 웨이브가 만든 history 동일토픽 중복쌍(cushion/snail/bb/thaad/indie/stylenanda/olive young/10-step/glass/amazon/tiktok/pdrn) → `EDITORIAL_DROP` set으로 빌드 제외 **+ 디스크/서버 stale 파일 수동 rm 필수**(생성기는 미생성 파일을 자동삭제 안 함). 결과 1003→**991 URL(982p)**, 링크 0깨짐. **K뷰티 최적화 W2(2026-06-26, 라이브, kbeauty.js v12·sw kp-v49)**: ①**허브 지연렌더** — modules/kbeauty.js boot()에서 25개 섹션별 render* 호출 전부 제거, 카테고리 첫 오픈 시 `ensureCatRendered(id)`(M맵: skin/routine/sun/ingr/trouble/brands/buy/trends→해당 렌더러들)가 1회 렌더(_renderedCats 가드). boot는 localize+renderTopAds+injectTrendSchema(SEO)+renderTicker+wireFilters+showShareFab만 = 모바일 TBT/INP 대폭↓. 프리뷰 8카테고리 전수 렌더 검증+딥링크(#cat=, #kb-섹션)+콘솔0. ②**내부링킹** — build-kbeauty-pages.cjs `kbCrossLinks(item)`(title/slug 단어경계 매칭으로 실제 ING/BRANDS 페이지 링크)+`deeperBlock()`(관련 가이드 + 인터랙티브 도구 CTA #cat=skin/routine/buy)를 판정페이지(snail→ingredient/snail 등)·편집글(ginseng→ingredient/ginseng 등) 본문에 주입. 절대링크 3259개 0깨짐. **W3 잔여(차단/저우선)**: YesStyle AWIN 딥링크 교체(승인 후 buyBox RETAILERS의 YesStyle u()만 교체하면 전 페이지 수익화 활성 — 현재 검색URL); 콘텐츠 9개 언어 현지화(현재 영문+hreflang).

**허브 최신 UX 편의 10종(2026-06-26, 라이브, kbeauty.js v13·OVERLAY_VER 8·sw kp-v51)**: modules/kbeauty.js `initUX()`(boot 끝에서 호출)가 추가: ①인스턴트 검색(#kb-toolbar 랜딩 상단, `kbSearchIndex`=window.KBEAUTY_INGREDIENTS/BRANDS/CONCERNS→/guide/kb/ 라이브러리 링크, 입력 필터+Enter 이동) ②맨위로(.kb-fabtop, scroll>600) ③읽기진행바(#kb-progress) ④모바일 하단탭(#kb-bnav, ≤680px, Menu/Skin/Routine/Buy→showLanding/showCategory, on 활성) ⑤토스트(`kbToast`) ⑥최근 본 항목(localStorage `kp_kbeauty_recent`, [data-ing]/[data-brand] 클릭 위임 기록→랜딩 칩, showLanding서 재렌더) ⑦스크롤 등장(IntersectionObserver .kb-sec.kb-reveal, reduced-motion 가드) ⑧카테고리 링크복사(#kb-share-cat 백바, location/kbeauty#cat=→clipboard+토스트) ⑨마이크로인터랙션(:active scale)+햅틱(navigator.vibrate(8)) ⑩퀴즈결과 CTA(renderResult: #kb-cta-stack→#kb-stack 스크롤, #kb-cta-routine→showCategory('routine')). 기존 테마토글(kpTheme)·공유FAB는 유지(중복 안 만듦). **UX 라벨 11종 9개 언어 현지화**(오버레이 `ux.*` 키, cx() fallback). showLanding/showCategory가 #kb-toolbar 표시·syncBnav 토글. 프리뷰 전수 검증(검색·최근·하단탭·토스트·퀴즈CTA·8카테고리·콘솔0). 9개 언어 리소스 200/valid 재확인.

**전용 브랜딩·단순화(2026-06-26, 라이브, sw kp-v52)**: ①허브 로고 `index.html`→`/kbeauty`(K·Beauty 워드마크 🧴, .kb-logo) — K뷰티가 독립 목적지. ②**여행 네비 chrome 제거** — `.hub-nav .hub-nav-links{display:none}` (드롭다운 Explore/Guides/Where-to-Go/Plan Trip 숨김; header.js가 `.hub-nav`에 append하는 **언어전환기·테마·검색은 보존**=안 깨짐). 작은 `KoreaPlus ↗` 부모링크(.kb-parent-link). ③**전용 파비콘** `kbeauty-favicon.svg`(핑크→라벤더 그라데이션 라운드+화이트 에센스드롭+글래스 하이라이트), theme-color `#d61f6e`. 허브+라이브러리 982p 전부 적용(generator shell). ④히어로 글래스스킨 radial-gradient. 프리뷰 검증: logo→/kbeauty·메뉴숨김·언어전환기/테마 보존·favicon·콘솔0. **언어별 신규 SEO 콘텐츠(2026-06-26, 라이브)**: 사용자 선택=언어별 신규 SEO(4유형 전부). **8개 비영어 언어(ko/ja/zh/es/fr/de/pt/id) × 28 캐노니컬 토픽 = 224p** 생성(`/guide/kb/<lang>/<topicKey>.html`). 토픽: 루틴5·how-to3·성분5·브랜드2·가이드1(선크림)·shop3·trend1·FAQ8. 멀티에이전트(언어당 1, effort high, wf wqyvzvh84)가 **네이티브 작성**(번역 아님, 시장 검색어 타깃)+출처·헤지·structure-function. 생성: 워크플로 output→`kbeauty-i18n.json`(루트)→build-kbeauty-pages.cjs **섹션15**가 페이지화. **토픽별 8개 언어 hreflang 클러스터**(I18N_HL 맵, x-default→hub) + **FAQPage 스키마**(AEO) + Article + BreadcrumbList, `<html lang=xx>`, 형제언어 링크. shell()에 `o.lang`/`o.altLinks` 파라미터 추가. **사이트맵 991→1215 URL**(1206p). 신규 토픽 추가 시 TOPICS 배열+워크플로 재생성. 영문은 기존 라이브러리(982p)가 커버(중복 회피). **+기업 심층분석 확장(2026-06-26, wf we3zv91h7)**: 한국 K뷰티 기업 14곳(아모레퍼시픽·LG생건·코스맥스·한국콜마·올리브영·COSRX·이니스프리·설화수·라네즈·조선미녀·미샤/Able C&C·클리오·스타일난다3CE·닥터자르트) × 8언어 = **+112p**(company-* topicKey), 같은 i18n 파이프라인. 공개사실만(매출/점유율 정성표현, 인수는 문서화된 것만=로레알-스타일난다·에스티로더-Have&Be), 기업별 8언어 hreflang. **+2차 기업확장(wf wytlumk7r)**: 돌풍 인디 12(Mediheal·Some By Mi·Anua·Round Lab·SKIN1004·Torriden·Numbuzin·TIRTIR·rom&nd·Banila Co·Purito·Isntree) + 뷰티디바이스 Medicube(APR, Age-R) + LG럭셔리 더 히스토리 오브 후 = +112p. **+3차(wf wic78qh5t)**: 페리페라·구달(클리오 서브)·Klairs·Axis-Y(글로벌인디)·편강율·닥터지(더마)·네이처리퍼블릭·토니모리(로드숍)·VT·달바·아비브·헤이미쉬·믹순·아임프롬 = +112p. **기업 총 42곳 × 8언어 = 336 기업분석 페이지**. 위키 없는 인디는 공식사이트 인용. **i18n 총 560항목(70토픽×8언어), 사이트맵 1439→1551 URL**.

**전 페이지 헤더배너 + 계층 드릴다운 네비(2026-06-26, 라이브)**: ①shell()에 좌상단 **헤더배너**(.kbh: 로고 🧴 K·Beauty→`/kbeauty` 홈 + 📚 Library→`/guide/kb/`, sticky) — 전 kb/ 페이지. ②**계층형 네비** — emit()이 INDEX 레지스트리(rel/dir/title, html의 h1 파싱) 기록 → 빌드 끝에 **카테고리 허브 23개**(`/guide/kb/<dir>/index.html`, dir별 페이지 전수 나열, HUB_META 맵) + **언어별 허브 8개**(`/guide/kb/<lang>/index.html`, 토픽 prefix로 그룹핑) + **최상위 인덱스 재작성**(카테고리 타일 5그룹 + 🌐언어 타일 + 판정 인라인, .kbhub-card/.kbhub-grid CSS). 구조: index→카테고리허브→페이지(EN), index→언어→언어허브→페이지(i18n) — **모든 페이지 홈에서 도달**. 절대경로 `/kbeauty`·`/guide/kb/` 사용(도메인 루트). 허브 index는 ads:false. 사이트맵 1551→1582(+31 허브). 신규 카테고리 추가 시 HUB_META에 dir 추가하면 자동 허브화.

**허브 지역별 현지화 + 캐시함정(2026-06-26)**: 사용자가 라이브 `/kbeauty?lang=pt`에서 티커 한글+쿠팡 노출 신고. **원인=스테일 캐시**(코드/오버레이는 정상, 로컬 프리뷰 ?lang=pt 완벽 재현: PT티커·쿠팡없음). renderTopAds 지역분기 강화: **ko=쿠팡파트너스(한국전용), 비-ko=글로벌 리테일러 strip(YesStyle/Amazon 검색링크, rel sponsored nofollow)** + AdSense는 전 언어. 캐시 무효화 관례 재확인: 오버레이/티커 변경 안 보이면 **OVERLAY_VER 범프(loadContent ?v=)+kbeauty.js ?v= 범프+sw kp-vNN** 3종 동시 범프 필수(CF 클린URL max-age=300, .js 11일 오리진캐시). lang 감지는 i18n.js·kbeauty.js 둘 다 ?lang= 우선으로 동일. 현재 OVERLAY_VER 9·kbeauty.js v14·sw kp-v54.

**티커 클릭 상세 + 헤더 가이드분리(2026-06-26, kbeauty.js v15)**: ①트렌드 티커(renderTicker)를 `<button class="tk-item" data-trend=id>`로 바꿔 클릭 시 `openTrendModal(id)`→기존 모달(#kb-modal/openModalA11y)에 **현지화 트렌드 상세**(emoji·title·verdict 배지·blurb·science·cite 링크 + "전체 트렌드" 버튼→showCategory('trends')) 표시. 9개 언어 작동(TRENDS는 localizeData로 현지화됨). ②**헤더 가이드 연관 제거** — `.kb-parent-link`(KoreaPlus↗→/guide/) `<a>` 요소 삭제(CSS는 잔존·무해), My Trip 버튼 `#kp-mt-btn,.kp-mt-btn{display:none}` 숨김(mytrip.js는 공유라 CSS로 페이지 스코프 숨김). K뷰티 헤더 = 로고(→/kbeauty)+검색+테마+언어만. 기업 추가 시 company workflow의 COMPANIES 배열 확장→재생성→kbeauty-i18n.json 병합(node merge, topicKey 중복제거)→build→deploy. 1·2차 워크플로 스크립트: kbeauty-company-deepdive-i18n(-2).

**고급 20요소 + 성능(2026-06-30, 전부 라이브, kbeauty.js v23·sw kp-v62)**: 선정 워크플로(7렌즈)로 20선. **라이브러리 page-type 신규(build-kbeauty-pages.cjs)**: dose/(효과%+pH, kbeauty-dose.json)·how-it-works/(작용기전 moa)·evidence/(GRADE 근거등급)·formulation/(제형과학)·best/(고민별 성분랭킹 ItemList, 데이터파생)·vs/(성분비교표 curated pairs)·matrix/pairing-matrix.html(N×N 페어링)·age/·climate/(연령·기후 루틴)·digest/(리서치 다이제스트)·watch/(규제/안전 워치)·hair/(K헤어/두피)·makeup/(K메이크업/퍼스널컬러)·clinic/(K더마 시술교육·강제 disclaimer). 콘텐츠는 워크플로 wgzknyym2(과학)·wmpxuvlpj(클러스터)가 무날조 작성→kbeauty-{dose,moa,evidence,formulation,adv-clusters}.json. **허브 도구(modules/kbeauty.js, 신규 'tools' 카테고리·KB_CATS+ensureCatRendered+openMoreSheet)**: renderCost(비용/use)·renderPAO(개봉후 PAO 만료일)·renderCycling(스킨사이클링 4박). **#16 임베드 위젯**: kb/embed/glass-skin-score.html(미니 iframe 타깃, raw write·사이트맵 제외) + tools/embed 랜딩(스니펫). #15 Skin-Index는 기존 Glass Score와 중복이라 병합. **#18 E-E-A-T**: shell()에 'KoreaPlus Editorial 검수·최종검토일' 바이라인 + reviewedBy 스키마(전 페이지). **성능**: ①페이지 CSS를 캐시 외부파일 kb/kb.css(CSS_VER)로 분리(인라인 ~4KB 제거)·②AdSense **idle-게이팅**(AD_BOOT: requestIdleCallback/첫상호작용 시 로드, head 렌더블로킹 제거, ins min-height:280 CLS≈0)·③신규 .cmp/.rank/.grade 스타일. **사이트맵 1852→2040.** 커밋 7269fec/ec381e0/4914a57. **후속완료(2026-06-30, 커밋 3ac25b2, OVERLAY_VER 11·js v24·sw kp-v63)**: ①**신규 도구·글래스스코어 14개 언어 현지화** — gs.*(45)+tool.*(49) 오버레이 + kbeauty.sec.cost/pao/cycling/glassscore+filter.tools 메시지(9)를 13에이전트 워크플로(w4c3a9i3g)로 번역→전 13언어 messages(+9)·오버레이(orig8 1090→1184, new5 1140→1189) 병합. 추출은 scratchpad/extract_loc.cjs(cx 정규식). end-to-end 검증(ko 허브 도구/글래스 네이티브). ②**레이어링 페이지 layer/ 14개**(#4 심화) — LAYER 랭크모델(thinnest→thickest·저pH우선·오일last)+LAYER_COMBOS 큐레이션으로 적용순서·대기시간·AM/PM분리·산+레티노이드 충돌 노트, HUB_META.layer. **사이트맵 2040→2055.** **남은 영어폴백**: 신규 영문 라이브러리 page-type(dose/vs/best/age/hair 등)은 영어우선 설계(i18n 토픽은 별도 네이티브)—의도. ⚠️매트릭스 슬러그는 index 금지(HUB_META 허브 index.html과 충돌→덮어씀); 단일 유틸 페이지는 비-index 슬러그로.

**Toss식 모바일 프레임워크(2026-06-27, 진행중)**: 6렌즈 병렬감사+종합 워크플로(kbeauty-mobile-framework)로 15항목·5배치 사양 도출(디자인토큰+패턴+우선순위). **근본문제: hub-styles.css:32 `body{overflow-x:hidden}`가 position:sticky를 깸**(헤더·티커·필터바 전부 스크롤로 사라짐). **Batch A+B 완료·라이브(sw kp-v57·kbeauty.js v16, 커밋 00d9e6e)**: ▶A(앱셸)= kbeauty.html inline에 `body{overflow-x:clip}`(공유 hub-styles 미수정·스코프 오버라이드)로 sticky 복구 + **:root 디자인토큰 레이어**(--kb-header-h:52/--kb-rail-h:54/--kb-bnav-h/--space-1~6/--r-sm~pill/--shadow-1~2/--card/--ease/--z-*) + 필터바를 **단일행 가로스크롤 칩레일**(flex-wrap:nowrap, 167px→54px, top:var(--kb-header-h)로 헤더 바로 아래 고정) + 티커 모바일 position:static(스크롤아웃) + 히어로 192→127px + .kb-sec scroll-margin=calc(header+rail+8). ▶B(네비/오버레이)= 모달을 **모바일 바텀시트**(@680 align-items:flex-end·둥근상단·그랩핸들 ::before·kb-sheet 키프레임·스와이프다운 닫기[#kb-modal touch, scrollTop0 게이트, 110px], 데스크톱은 중앙 다이얼로그 유지) + **바텀탭 5슬롯**(Home/Skin/Routine/Shop/More, initUX items) + **openMoreSheet()**(나머지 sun/ingr/trouble/brands/trends+Library를 바텀시트로) + syncBnav가 KB_BNAV_PRIMARY 외엔 'more' 점등(항상 한 탭 활성) + 하단 고정요소 오프셋 calc(--kb-bnav-h) + 16px 입력(iOS 줌방지). **검증: 390/1200px sticky·바텀시트 안착·가로넘침0·콘솔0·데스크톱 회귀0**. ⚠️**프리뷰 헤드리스 렌더러는 CSS 애니메이션을 0프레임에 동결**(스크린샷/rAF 타임아웃, 바텀시트가 translateY(100%)로 측정됨)→실브라우저는 정상, computed-style로 검증할 것. **Batch C+D+E 완료·라이브(2026-06-27, kbeauty.js v19·sw kp-v58, 커밋 5378646)**: ▶C(밀도·성능): Trends 8섹션→핵심4(radar/ledger/viral/board) 노출+권위4(report/news/krsrc/trust) `applyTrendsDensity()` 확장버튼 뒤로(딥링크 시 opts.activeSec로 자동펼침); CLS 예약높이 #kb-filters54/#kb-landing340; boot()의 비시각작업(injectTrendSchema·kbtrack·observeImpression) `requestIdleCallback` 지연(renderTopAds·renderTicker는 즉시=수익·CLS 보호); 4 스크립트 defer. **localize per-cat 재아키텍처는 위험>이득으로 의도적 제외**(lazy-render가 이미 TBT 해결). ▶D: 테마인지 카드 elevation(.kb-ing 등에 var(--shadow-1), 다크=미세그림자+1px 라이트링·라이트=핑크 소프트); 전체 탭타깃 :active scale(.97)+navigator.vibrate 확대. ▶E: **전용 PWA** `kbeauty-manifest.json`(id/kbeauty·start_url=/guide/kbeauty.html?src=pwa[SW스코프내=오프라인]·scope:/[전체 인앱]·핑크·아이콘 PNG 192/512/maskable·단축4)+**sharp로 SVG→PNG 4종 생성**(kbeauty-icon-192/512/maskable+apple-touch180, 핑크 그라데+화이트드롭)+apple-mobile-web-app 메타+viewport-fit=cover+SW precache; 랜딩 1차 CTA `.kb-cta-hero`(i18get('kbeauty.sec.quiz.title/sub') 9개언어 재사용→showCategory('skin')+jumpTo('#kb-quiz')); rAF 스크롤 스로틀; 모바일 티커 calm(마퀴 제거·scroll-snap·visibilitychange 일시정지·will-change제거); 2단계 섹션헤더(모바일 세로적층). **검증: 모바일390+데스크톱1280 회귀0·라이트/다크 토큰·콘솔0·매니페스트 유효·아이콘 4종 200**. **의도적 제외/보류(되돌리지 말 것 아님, 후속 가능)**: rank15 광범위 핑크 그라데 절제(스크린샷 불가로 시각검증 못해 보류), 커스텀 설치 pill(shared pwa.js와 중복위험). ⚠️**프리뷰 함정**: ①해시만 바꾸면 리로드 안 됨→`?t=Date.now()` 쿼리로 강제내비 ②JS 편집 후 `?v=N` 범프해야 HTTP캐시 우회 ③SW가 구버전 캐시→preview서 unregister+caches.delete. 사양 JSON: 워크플로 wf_9c0e5aa8-0c5 결과.

**AdSense 우선 전환 + 다크모드 수정(2026-06-27, 라이브)**: 사용자 요청="쇼핑 유도 줄여라, 어필리에이트보다 AdSense 위주, 어필리에이트 보류". ▶**어필리에이트 보류 site-wide**: modules/kbeauty.js `const AFFILIATE_ON=false`(허브) + build-kbeauty-pages.cjs `const AFFILIATE_ON=false`(라이브러리) 두 플래그. 허브 renderTopAds 쿠팡/YesStyle/Amazon 제거→AdSense 전용; 곳곳 🛍️Shop CTA(.kb-step-shop/.kb-shelf-cta/모달 data-seed/.funnel-disc) `body.kb-noaffil`로 CSS 숨김; 라이브러리 buyBox() 무출력(전 1000p). **재개=플래그 true 둘 다.** 바텀탭 'Shop'(buy)→'성분'(ingr), buy는 More 시트로. ▶**다크모드 가독성**: 대비비 자동감사(JS, luminance/contrast)로 진단→상태배지 흰글자가 중간톤 배경서 2.6~3.9. 배지 bg 어둡게(일괄치환 #9aa0a6→#5f6571, #27ae60→#1a7a45, #2f80ed→#2060c8, #e0823a→#b35f1e, st-mainstream #8b46d6→#7a3bc4) + 보라링크·경고문 `html:not(.light)` 밝힘 → ≥4.5(AA). 커밋 3774f4b. sw kp-v59·js v20.

**트래픽·한국홍보 10요소(2026-06-27, 워크플로 wf_a46c23b1-89b 선정)**: 6렌즈→10선(영향×노력). **6/10 라이브**: #1 Quick-answer Speakable(shell()에 한국프레임 .qa + WebPage SpeakableSpecification 전 ~1000p 일괄, o.quickAnswer||o.desc, 허브 제외)·#3 alternative/(글로벌제품명 역키워드 'Korean alternative to X' 23p, KBEAUTY_DUPES reference 그룹)·#4 say/(발음+한글 38p: 30브랜드+8성분, kbeauty-say.json)·#5 hanbang/(한방 유산 12종, kbeauty-hanbang.json)·#6 ask/(음성/PAA 질문URL 39p, kbeauty-ask.json)·#9 report/(자동날짜 월간 트렌드리포트, VELOCITY+BESTSELLERS 조인). 콘텐츠는 워크플로 wf_a9dcde74-93e가 무날조·출처기반 작성. **10요소 전부 완료·라이브(2026-06-27)**: 추가로 #2 언어별 llms-kbeauty.<lang>.txt(build-llms.cjs가 kbeauty-i18n.json 루프, 네이티브 페이지 인덱스+검증 moat) + answer-ledger.json(kb/, 22 검증, AI크롤러)·#7 star/(K-pop/K-드라마 뷰티 **일반 교육 5p만**, 특정인 제품주장 X=right-of-publicity 안전, /kpop 상호링크)·#8 **Glass Score**(허브 skin탭 #kb-glassscore, 8문항→0-100+4세부바[수분/장벽/광채/꾸준함]+한국등급 젤리🫧/허니🍯/글래스🔮+다음단계3, kbeauty-sharecard.js generateGlassScore 공유카드, /guide/kb/glass-score/ 랜딩, kbeauty.js v21·sharecard v2)·#10 **+5개 언어**(ar RTL/hi/ru/vi/th × 28토픽 네이티브=140p, 워크플로 wf_c1f9c6f1-504, kbeauty-i18n.json languages 13개, LANG_NAMES/I18N_HL 확장, shell() ar dir=rtl). **라이브러리 9→14개 언어, 사이트맵 1582→1852.** 커밋 a8513ce/a159051/f1f423f/e4c385d/82aae60. **신규 page-type 추가법**: build-kbeauty-pages.cjs에 emit 루프 + HUB_META 항목 + (데이터는 kbeauty-{say,hanbang,ask}.json 같은 별도 JSON). **생성기 배포=tar cf(무압축, czf는 Windows서 비정상 느림; 단 디스크경합/백그라운드 동시tar 시 느려짐→포그라운드 단독)→scp(10초)→ssh `tar xf`+개별 chmod(전체 find chmod는 1843파일서 5분초과 위험, 신규 dir만 chmod), OUT=kb/.** **신규 5개 언어 허브 완전 현지화 완료(2026-06-27, kbeauty.js v22·OVERLAY_VER 10·sw kp-v61, 커밋 f45ba9e)**: messages/{ar,hi,ru,vi,th}.json(en 베이스 위 226 허브+크롬키 오버라이드, 614키 파일) + assets/kbeauty-content.{ar,hi,ru,vi,th}.json(1140키 오버레이=en우선 병합 es 1090 + gs.* 50, 글래스스코어 포함) — 15에이전트 워크플로 wf_f4ad2cee-202가 네이티브 번역. i18n.js SUPPORTED+LABELS(국기+네이티브명)→스위처 14개 자동, kbeauty.js _SUPPORTED +5. **아랍어 RTL은 전역 dir 금지(공유 여행 크롬 cookie/search/trip이 RTL 비대응→가로오버플로 8829 발생)하고 허브 자체 컨테이너에만 스코프**(html[lang=ar] .hub-nav/.kb-ticker-wrap/.hub-hero/main.hub-content/#kb-modal/#kb-bnav{direction:rtl} + 고정요소 미러). i18n.js는 document dir 항상 ltr 유지. 라이브러리 페이지는 generator shell이 자체 dir=rtl(공유크롬 없음). 검증: ar 허브 네이티브+RTL·th 네이티브·오버플로0·콘솔0. **라이브러리 14개 언어 + 허브 14개 언어 완전 현지화.** ⚠️신규 5개 언어 messages는 여행앱 네임스페이스(hub/plan/trip/kpop/tb/chat/dash/dp) 제외 → 여행 페이지는 영어 폴백(K뷰티 허브만 타깃). sw PRECACHE에 신규 5개 messages/overlay 미등록(런타임 cache-first가 처리, 오프라인 후속). **사용자 명시 product 기본값(되돌리지 말 것): 바텀탭=Home/Skin/Routine/Shop/More, More는 시트, 랜딩 1차CTA=스킨퀴즈(퍼널→Shop), 티커 유지(모바일 calm).**

**허브 글자 +1px(2026-06-26, sw kp-v56)**: 사용자가 트렌드 상세 글자가 작다 하여 kbeauty.html 인라인 `<style>`의 **모든 `font-size:Npx`를 +1px 일괄 증가**(node 정규식 `font-size:([0-9.]+)px`→+1, 222개 치환, clamp 1건은 `font-size:clamp(`라 미매치=안전). CSS라 9개 언어 자동 동일 적용. 데스크톱·모바일(390px) 가로넘침0·작은배지/칩 클리핑0 검증. 추가 조정 시 같은 정규식으로 +N. **사용자 명시 선호니 되돌리지 말 것.**

**수익화 정리(2026-06-21, sw v46)**: 사용자 요청으로 **AliExpress 전부 제거**(renderTopAds 스트립·loadTopAli 삭제·loadShop은 jumpTo('#kb-buy')로 중립화·#kb-shop 섹션+Shop필터칩 제거·KBEAUTY_BUY에서 aliexpress 판매처+affiliate키 제거). **쿠팡 캐러셀(ko, id 996633/AF4227535)은 Wave A 커밋에서 실수로 regression됐던 것을 renderTopAds+injectCoupang으로 복원**(.kb-ad-coupang CSS도 복원). 최종 K-뷰티 광고: ko=쿠팡+AdSense, 그외=AdSense만. 여행=Klook(modules/klook.js)+AdSense. AliExpress는 나중에 필요시 재요청 예정(워커 ALI_APP_KEY/SECRET 미설정 상태, aliexpress.ts 코드는 tracking_id 'luckynum'로 보존). **주의: 이 작업들은 git 브랜치 `redesign/app-ui`에서 커밋됨(메인 아님). 리디자인(index.html+style.css pass-1)+kpop WIP가 같은 브랜치에 있음. 라이브 배포는 scp라 브랜치 무관하게 반영됨. 머지시 main으로.**

**상단 스폰서 광고 블록 완료(K-뷰티 페이지, 히어로 직하 #kb-topads)**: renderTopAds()=AliExpress geo 상품스트립(API.getKbeautyProducts, 키 없으면 정품허브로 보내는 폴백배너)+AdSense(ca-pub-1378943893051810/slot 4521899200 재사용)+한국어(lang==='ko')만 쿠팡파트너스 캐러셀(injectCoupang: about:blank iframe에 document.write로 g.js 주입=currentScript 우회, id 996633/trackingCode AF4227535=계정단위 도메인무관, 법정고지문 포함). 라벨/CTA 9개국어. AdSense는 페이지 기존 로더 재사용+push. ALI_APP_SECRET 미설정이라 현재 AliExpress는 폴백배너(키 들어오면 자동 상품스트립).
**URL(클린 URL 서빙, 2026-06-15)**: /kbeauty는 **리다이렉트가 아니라 워커가 프록시 서빙**한다 — src/worker.ts에서 path==='/kbeauty'면 origin https://koreaplus-lifes.com/guide/kbeauty.html를 fetch→HTML의 <head> 뒤에 `<base href="/guide/">` 주입→200 반환(주소창이 /kbeauty 그대로 유지, 상대경로는 base로 /guide/에 해석). 실패시 302 폴백. /k-beauty→301→/kbeauty. 워커 배포: cd koreaplus-webapp && npx wrangler deploy(OAuth jeybeeicon, 워커명 koreaplus-webapp). 깨질 위험이던 유일한 bare-fragment `href="#kb-shop"`(shelf CTA)는 JS 스크롤로 변경(base와 충돌 방지). canonical/og:url을 https://koreaplus-lifes.com/kbeauty로 통일. i18n switchLang은 location.href 기반이라 /kbeauty?lang=xx로 깔끔 유지(replaceState). /kbeauty는 SW scope(/guide/) 밖이라 SW 미제어=항상 fresh.
**트렌드&권위 Top-10(2차 고도화, 2026-06)**: 다중에이전트 설계 워크플로로 10개 확정(설계 결과는 세션 transcript). 빌드 순서 Wave A(P0: #1,#2,#3)→B(P1: #7,#4,#5)→C(P2: #6,#9,#8,#10). **완료·라이브(sw kp-v36, cache-bust ?v=4)**: #1 서울vs세계 트렌드레이더(KBEAUTY_RADAR 14트렌드, kr/we 0-100 좌표 SVG 2×2 사분면+stage/momentum/crossover 칩+verdict+날짜+방법론, renderRadar), #2 근거원장(KBEAUTY_TRENDS에 verdict+science+cite 추가, KBEAUTY_CITATIONS=PubMed/AAD 실제 URL, renderLedger, science는 localizeData line52 trend.<id>.science 경유). **트렌드&권위 Top-10 전부 라이브 완성(2026-06-18, sw kp-v39, cache-bust ?v=7, OVERLAY_VER 7, 오버레이 1079키)**: #1 레이더, #2 근거원장, #3 신뢰센터(KBEAUTY_TRUST, 방법론·등급기준·출처·신선도·실명은 KoreaPlus Editorial Team으로 정직하게=가짜 의사명 안씀, 자동 last-updated표, dateModified 2026-06-18), #4 스킨톡 리얼리티체크(KBEAUTY_VIRALCHECK 10건 claim/science/note/velocity), #5 뷰티과학·안전 데스크(KBEAUTY_NEWSWIRE 큐레이션=실제 권위기관 링크 AAD/FDA/CosIng/MFDS/PubMed, 가짜 헤드라인 아님), #6 채널속도보드(KBEAUTY_BESTSELLERS_VELOCITY 16종에 채널/koreaNative/status/whyMoved, renderBestsellers에 Korea-native 필터+상태칩 통합), #7 언어별 ItemList JSON-LD 주입(injectTrendSchema)+신선도, #8 한국출처 출처표기(KBEAUTY_KR_SOURCES 5개 명시·날짜·무스크래핑), #9 맞춤피드("내 피부 맞춤" 토글=personalFilter, KBEAUTY_TREND_TAGS로 레이더+스킨톡 필터), #10 분기리포트+공유(KBEAUTY_REPORT, renderReport, navigator.share). 9개 언어 전부 현지화. **2차 고도화 100% 완료.**
**현지화 파이프라인 핵심**: 신규데이터 산문은 kb_extract2.cjs(ALLOW 화이트리스트 워커, DS 목록에 데이터셋명 추가)로 추출→델타(ko오버레이에 없는 키)만 _t2d_en.json으로 뽑아 번역워크플로(언어당 1에이전트, agentType general-purpose, _t2d_<lang>.json 작성)→kb_merge_delta.cjs로 오버레이 병합. **신규 산문 필드명은 ALLOW와 modules/kbeauty.js의 NC_ALLOW 둘 다에 추가해야 번역됨**(예: 이번에 'methodology','science' 추가; enum라벨맵은 SPECIAL_OBJ/NC_SPECIAL에 'stageLabels' 등 추가). 섹션 헤더(data-i18n)는 messages/<lang>.json 9개에 직접 추가(누락시 raw key 노출).
**배포 주의**: 백그라운드 scp가 최신파일 못 올린 적 있음 → 포그라운드 scp로 재배포해 origin을 curl로 직접 검증할 것. .js는 11일 캐시라 kbeauty.html의 ?v= 올려야 클린URL 갱신.

**주의**: /guide/kbeauty(확장자 없음)은 WP가 모르는 경로라 홈(/)으로 튕김 → 서버 실제 디렉터리 /guide/kbeauty/index.html(meta+JS로 **/kbeauty** 리다이렉트, ~470B)로 해결(Apache DirectorySlash가 WP보다 먼저). mkdir 후 scp로 index.html 반드시 같이 올릴 것(빈 디렉터리면 403). 정적 배포는 narrow sudo chown bitnami:daemon + scp(kbeauty.html,sw.js,modules/kbeauty.js,8 overlays,kbeauty/index.html). **PWA SW는 v34부터 install에서 self.skipWaiting()→배포시 다음 방문에 자동 갱신(수동 캐시삭제 불필요), 매 배포 CACHE 버전 kp-vNN 올릴 것.** 신규 섹션 패턴: kbeauty-data.js 데이터→kbeauty.html 빈 컨테이너+필터칩+i18n키(en/ko)→modules/kbeauty.js 렌더함수+boot+필터맵→CSS. 배포: 변경 루트파일 `sudo chown bitnami:daemon`(개별) 후 scp.

**함정**: ①언어파일에 키 추가 시 SW가 구버전 메시지JSON 프리캐시해 stale → **CACHE 버전 범프 필수**(kp-v24 현재). ②nav 3중 복제(build-seo PRIMARY_NAV + 루트허브 인라인 + 생성페이지는 build-seo 재생성) — 항상 동기화. ③생성 SEO출력(places/guide/itinerary/blog/faq/ja/zh/es/explore.html/llms.txt)은 gitignore. ④build-seo.cjs OUT=__dirname, 네트워크/env 무의존. 관련: [[koreaplus-kpop-vertical]] [[lucky-deploy-pipeline]].
