---
trigger: model_decision
description: "Rule for koreaplus-webapp: religion-webapp.md"
---
Vertical **"종교의 모든것 / All About Religions"** — **DEPLOYED LIVE at all-lifes.com/religion** (2026-07-06). Repo `C:\Users\juksu\religion-webapp` (branch `main`, user alllifes77-hue). Plan: `C:\tmp\all-lifes-repo\docs\religion-webapp-plan.md`.

**Architecture = static SSG** (same as pet/calc/plants/supplements, NOT the lucky worker model). All pages pre-rendered to `dist/religion/` and deployed to Lightsail Bitnami docroot `/opt/bitnami/wordpress/religion` (Seoul box 3.36.235.171). An interactive SPA is served at `/religion/`. There is NO Cloudflare worker (the first-pass worker was removed).

**URL scheme:** `/religion/` = SPA (English canonical). `/religion/{lang}/` = static hub. `/religion/{lang}/{id}/`, `/compare/{a}-vs-{b}/` (alphabetical pair order!), `/calendar/`, `/holiday/{id}/`. Sitemap `/religion/sitemap.xml` = **990 URLs** (9 langs × 110). llms.txt + robots.txt under `/religion/`.

**Files:** `data/religions.js` (12 religions, neutral EN+KO, UMD single source), `data/holidays.js` (30 holidays + date resolver: fixed/Meeus-Easter/lunar-tables 2026–2030), `render.js` (shared page builder — buildHub/Detail/Compare/Calendar/**Holiday**/Sitemap/Llms, reads globals), `build.cjs` (SSG → dist/religion/), `lang/en.js`+`ko.js` (UI strings + ref names), `index.html`+`religion-app.js`+`religion-style.css` (SPA, hash router, dark mode), `deploy-religion.sh`, PWA files.

**Deploy:** `bash deploy-religion.sh` (build → tar → scp → sudo extract → chmod a+rX loop → verify 200s → chunked IndexNow). Needs PEM `/c/Users/juksu/LightsailDefaultKey-ap-northeast-2.pem`. IndexNow key `3b23ebbe6ef3f8d86a1d70a7ca435527` (domain-wide, chunk 100/req — full 990 gets 400). Still TODO: submit sitemap in Google Search Console.

**Killer traffic engine = holiday pages** ("when is Ramadan 2027" → 5-year date table + countdown). Verified live: en/ko hubs, detail, holiday, compare, calendar all 200; 5337 internal links, 0 broken.

**Content enrichment (2026-07-06, done):** detail pages are now ENCYCLOPEDIA-GRADE. Each religion has overview (multi-para), history, 8 beliefs, 6 practices, key figures, branches explained, demographics, 6-item FAQ (FAQPage schema, targets real queries), 8-term glossary, etiquette — EN + natural KO. Lives in `data/content.en.js` (148KB) + `data/content.ko.js` (162KB), assembled by `build-content.cjs <rcontentDir>` from per-religion JSON. Authored by a Workflow (12 author + 12 adversarial-verify agents; verifiers caught a fabricated bahai branch, MT-like Korean, doctrinal phrasing). render.js buildDetail + SPA renderDetail consume `globalThis.RELIGION_CONTENT` with base-data fallback. To add content langs: add to CONTENT_LANGS in index.html + write content.{xx}.js. Detail pages ~2.5x bigger (islam 10.5→26KB). Assets bumped to ?v=2.

**To extend:** add religion → append RELIGIONS (+holidays +lang names), all regenerates. Add language → lang/{xx}.js + ALL_LANGS in render.js + i18n.{xx} per religion + SEO.{xx}. Roll holiday dates yearly (dates tables + YEARS in render.js).

**Global redesign v4 (2026-07-09, LIVE):** sitemap now **1017 URLs** (990 + timeline/statistics/places × 9 langs). Wikimedia Commons imagery via `fetch-wikimedia.cjs` → `data/images.js` (73 curated hero images: 12 religions/29 holidays/32 places, hotlinked upload.wikimedia.org thumbs + attribution/license; respectful-imagery = architecture/festival objects only, no figurative Muhammad subjects). Hero banners + photo credit on detail/holiday pages; religion Wikimedia photo = og:image; hub tiles + SPA cards have thumbs. Depth pages from `data/extras.js` (assembled by `build-extras.cjs` from workflow-authored timeline.json/statistics.json/places.json — 28 events, Pew-based stats analysis, 32 place cards w/ visit etiquette incl. Mecca/Medina non-Muslim entry note). Compare pages have derived "At a glance" auto-analysis (koWa 와/과 particle helper). Detail has quicknav + Explore-next recommendations. Assets ?v=4 / SW religion-v4. To refresh images: `node fetch-wikimedia.cjs` (Commons search API, UA required).

**AdSense (2026-07-06):** ca-pub-1378943893051810, slot 4521899200, auto/responsive (same as pet). Placed TOP + BOTTOM on content pages only (detail/compare/calendar/holiday); hub & SPA home are ad-free. Static: render.js shell `ads` flag (buildDetail/Compare/Calendar/Holiday pass ads:true; buildHub does not). SPA: `mountAds()` in religion-app.js router injects top+bottom slots on content views + lazily loads the AdSense script (home never loads it). Label localized (AD_LABELS). Assets at ?v=3 / SW religion-v3.

**Guardrails (hard):** third-person neutral voice, no faith ranking/conversion/belief-judging quiz, never depict Muhammad, sourced figures, ±1-day lunar caveat. **Next phases:** 9-lang content localization, sacred-site guides wired to Agoda/Klook affiliate (monetization angle), FAQ pages. Local SPA dev server: scratchpad `relserve.cjs`, launch config "religion" port 4176. Sibling apps same box: [[pet-webapp]], [[calc-webapp]], [[plants-webapp-plan]], [[supplements-webapp-plan]], [[dino-webapp-plan]].
